import { VTuber } from "../data/vtubers";
class VTuberInstance {
    get Level() {
        VTuber.get_by_id(ID).x;
    }
}
class Party {
    /**
     * Combined strength of all party members.
     */
    TotalStrength() {
        this.Members.reduce((vtub) => vtub.);
    }
}
//# sourceMappingURL=world_entity.js.map